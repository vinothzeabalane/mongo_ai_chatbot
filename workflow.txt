mongo_ai_chatbot/
├── app.py                ← Streamlit entry point
├── config.py             ← MongoDB + Ollama settings
├── requirements.txt
├── workflow.txt
├── testing.txt
├── .gitignore
├── core/                 ← query logic
│   ├── parser.py         ← NL question → Query object
│   ├── query_builder.py  ← Query → Mongo pipeline
│   ├── query_engine.py   ← orchestrates execute()
│   └── mongo.py          ← MongoDB connection
├── ai/
│   └── llm.py            ← Ollama fallback for off-topic questions
├── ui/
│   ├── formatter.py      ← result → readable text / table rows
│   └── charts.py         ← result → Streamlit line chart
└── tests/
    └── test_smoke.py     ← automated regression suite


User question
     │
     ▼
 parser.py → is_dashboard_question()
     │
     ├── YES: dashboard question
     │         │
     │         ▼
     │     query_engine.py     ← orchestrates the pipeline
     │         │
     │         ▼
     │     query_builder.py    ← builds Mongo aggregate pipeline
     │         │
     │         ▼
     │       MongoDB
     │         │
     │         ├── tabular results ──► formatter.py ──► Streamlit UI
     │         │
     │         └── chart results ────► charts.py ─────► Streamlit UI
     │
     └── NO: off-topic question
               │
               ▼
           llm.py → Ollama (llama3.1:8b)
               │
               ▼
           Streamlit UI  (plain answer)


Sidebar
- "Show debug info" checkbox gates: Detailed view, MongoDB Query, Debug expanders.


Routing Decision (is_dashboard_question)
- Any UPPER_CASE_WITH_UNDERSCORES token → dashboard  (e.g. MAINFW_CORESIGHT_INIT)
- Any keyword from DASHBOARD_KEYWORDS       → dashboard  (host, metric, boot, spi …)
- Anything else                             → LLM fallback

Routing Examples
  Question                                    Route      Metric
  Show MAINFW_CORESIGHT_INIT metrics          dashboard  MAINFW_CORESIGHT_INIT
  show MAINFW_BSS_INIT for rc136-031-19-s3   dashboard  MAINFW_BSS_INIT
  highest PBL_LOAD_MAIN_FW                   dashboard  PBL_LOAD_MAIN_FW
  show TBL_PCIE_INIT for EB0                 dashboard  TBL_PCIE_INIT
  what is python                             LLM        —

Known Metrics (KNOWN_METRICS in core/parser.py)
  SBL: SBL_SPI_INIT, SBL_FCONFIG_LOAD, SBL_CRYPTO_INIT, SBL_CRITICAL_BOOT_LOAD,
       SBL_UFH_LOAD_AND_VERIFY, SBL_DIGEST_COMPUTE, SBL_LOAD_TBL_IMAGE, SBL_RIOT, SBL_TOTAL
  TBL: TBL_PRETOTAL, TBL_SPI_INIT, TBL_FCONFIG_LOAD, TBL_PCIE_INIT,
       TBL_LOAD_PBL_IMAGE, TBL_RIOT, TBL_TOTAL
  PBL: PBL_SPI_INIT, PBL_PARSE_FCONFIG, PBL_PMIC_INIT, PBL_DRAM_INIT,
       PBL_SCRUB_MAINFW_DRAM, PBL_CRYPTO_INIT, PBL_NAND_INIT, PBL_LOAD_MAIN_FW,
       PBL_RIOT, PBL_WAKE_CORES_JUMP, PBL_TOTAL
  MAINFW: MAINFW_BSS_INIT, MAINFW_PCM_BSS_INIT, MAINFW_SPI_SEC_INIT,
          MAINFW_ENABLE_ADDR_PARITY_CHECKING, MAINFW_UART_INIT, MAINFW_GPIO_INIT_LOCKS,
          MAINFW_GPIO_INIT_DCSU, MAINFW_BL_SPINLOCK_SETUP, MAINFW_LLOG_INIT,
          MAINFW_MGR_REG_INIT, MAINFW_MEM_PRINT_INIT, MAINFW_PRE_PMU_INIT,
          MAINFW_GIC_LOCK_INIT, MAINFW_APP_TIMER_LOCK_INIT, MAINFW_ULOG_INIT,
          MAINFW_PMU_INIT, MAINFW_SMBUS_INIT, MAINFW_INT_INIT, MAINFW_RNG_INIT,
          MAINFW_KERNEL_INIT_EARLY, MAINFW_PS_INIT_0, MAINFW_PS_INIT_1, MAINFW_PS_INIT_2,
          MAINFW_DRAM_SCRUB_WAIT, MAINFW_DRAM_SCRUB_3, MAINFW_DRIVER_SUB_INIT,
          MAINFW_TASK_CREATE, MAINFW_SAVE_AND_CLEAR_BRAM, MAINFW_CORESIGHT_INIT
  Totals: BOOTLOADERS_TOTAL, OVERALL_TOTAL

Notes
- parser.py uses deterministic regex/keyword rules; LLM is only used for off-topic questions.
- Metrics live in nested Mongo fields: data.<METRIC_NAME>.min / .max
- config.py holds MongoDB and Ollama connection settings (env-var overridable).
- tests/test_smoke.py is the automated regression suite, run with: python -m pytest tests/test_smoke.py -v